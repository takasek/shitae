// r2: shadow 一致単位の実測 — quote 剥ぎ・? 無視・空白正規化（ADR-0006 B6）
# 設定
共通ラベル
> タップ("閉じる") -> back()
> 長押し(項目) -> back()
> ダブル タップ(項目) -> back()
## 編集
編集バー
> タップ(閉じる) -> goto(##通常)
> 長押し(項目?) -> goto(##通常)
> ダブル  タップ(項目) -> goto(##通常)
## 通常
表示
