// C08: result-list の境界。
// e: 空の結果列（`->` の後に何も無く、継続行も来ない）
// f: 同一ラベルの再出現（[成功] A ; [失敗] B ; [成功] C — 「次の別のラベルまで」の字義で C は?）
// spec 字義: e は EBNF 上 result-list が1個以上を要求 → 構文エラー? f は「別のラベル」の定義が曖昧（⚠️ 予測）。

# 画面
保存
> タップ(保存) ->
> 長押し(保存) -> [成功] goto(詳細) ; [失敗] エラー表示 ; [成功] ログ送信   // f

# 詳細
本文
