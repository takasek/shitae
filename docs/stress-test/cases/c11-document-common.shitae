// r2: document common の境界 — 前方参照・3 階層 shadow・継続行・component##variant 形
> プッシュ通知をタップ -> push(記事詳細)
> セッション切れ -> exit(@loggedIn)
> 共有 -> [シート] present(共有シート)
>        [キャンセル] 何もしない
> タップ(戻る) -> back()
> 深いリンク -> goto(ホーム##編集中)

# ホーム
> タップ(ログイン) -> present(記事詳細, @loggedIn)
> タップ(戻る) -> goto(##編集中)
## 通常
記事一覧
## 編集中
編集バー
> タップ(戻る) -> goto(##通常)

# 記事詳細
本文

# 共有シート
共有先一覧
