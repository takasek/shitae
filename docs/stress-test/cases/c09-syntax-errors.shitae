// C09: 明文化済み構文エラーの実測確認。
// a: -> の重複（矢印2つ）
// b: 先行インタラクションなしの継続行（この component の先頭 > 行が -> を含まない）
// spec 字義: どちらも構文エラーと明記 → error を期待。

# 画面
> ただの結果列                       // b: 先行なし継続行 → 構文エラーのはず
> タップ(保存) -> goto(詳細) -> back()   // a: 矢印2つ → 構文エラーのはず

# 詳細
本文
