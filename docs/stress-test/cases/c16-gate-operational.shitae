// r2: presence gate の操作的定義 — alias 名/実体名・member 参照・未定義対象・variant 差
# 日付
## 選択可能
選択可能ラベル
> タップ(日付) -> 選択する
## 満席
満席表示

# 予約
*日付
枠: 予約枠
> タップ(日付.選択可能ラベル?) -> push(時間選択)
> タップ(枠?) -> push(時間選択)
> タップ(予約枠?) -> push(時間選択)
> タップ(未配置?) -> push(どこか)
## 通常
カレンダー
## 絞り込み
フィルタ
> タップ(フィルタ?) -> 絞り込む

# 時間選択
時間一覧

# どこか
何か
