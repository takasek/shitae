// r3: singleton + member presence gate — gate は共有 variant の現在値で判定されるはず
#! クーポン
## 未受取
受け取るボタン
> タップ(受け取るボタン) -> goto(##受取済)
## 受取済
適用ボタン

# ホーム
クーポン
> タップ(クーポン.適用ボタン?) -> push(決済)
> タップ(開く) -> present(クーポン)

# 決済
完了
